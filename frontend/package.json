{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test --test-isolation=none server/*.test.mjs",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "email:server": "node server/dealScoutEmailServer.mjs",
    "email:send": "node server/sendDealScoutDigest.mjs",
    "start:email": "node server/dealScoutEmailServer.mjs",
    "scout:digest:preview": "node server/runAutomatedDealScoutDigest.mjs",
    "scout:digest:send": "node server/runAutomatedDealScoutDigest.mjs --send"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.9"
  },
  "dependencies": {
    "resend": "^6.12.4"
  }
}
